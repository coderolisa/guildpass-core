{
  "name": "@guildpass/shared-types",
  "version": "0.1.0",
  "private": false,
  "type": "commonjs",
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "typecheck": "tsc -p tsconfig.json --noEmit",
    "lint": "echo \"No linter configured for shared-types\"",
    "test": "echo \"No tests for shared-types\" && exit 0"
  },
  "devDependencies": {
    "typescript": "^5.4.0"
  }
}
